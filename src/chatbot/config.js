import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage("Welcome to Influencer Hub! How can I assist you today?")],
  botName: "InfluencerBot",
};

export default config